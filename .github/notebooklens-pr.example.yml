name: NotebookLens PR Example

on:
  pull_request:
    types: [opened, synchronize, reopened]

permissions:
  contents: read
  pull-requests: write

jobs:
  notebooklens:
    runs-on: ubuntu-latest
    steps:
      - id: notebooklens
        name: Run NotebookLens (none mode first)
        uses: notebooklens/notebooklens@v0
        env:
          GITHUB_TOKEN: ${{ github.token }}
        with:
          ai-provider: none
          redact-secrets: true
          redact-emails: true

      # Optional repo-level reviewer playbooks live in .github/notebooklens.yml.
      # They are not action inputs and still work in none mode.

      # Optional second step: enable Claude after none-mode baseline is validated.
      # - name: Run NotebookLens (Claude mode)
      #   uses: notebooklens/notebooklens@v0
      #   env:
      #     GITHUB_TOKEN: ${{ github.token }}
      #   with:
      #     ai-provider: claude
      #     ai-api-key: ${{ secrets.ANTHROPIC_API_KEY }}
      #     redact-secrets: true
      #     redact-emails: true
